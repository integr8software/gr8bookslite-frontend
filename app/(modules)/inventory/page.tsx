import { ModulePlaceholderPage } from "@/app/src/ui/modules/shared/ModulePlaceholderPage";

export default function InventoryPage() {
  return (
    <ModulePlaceholderPage
      eyebrow="Modules"
      title="Inventory"
      description="Inventory is routed and branch-aware inside the new ERP shell, ready for stock and warehouse pages."
    />
  );
}
