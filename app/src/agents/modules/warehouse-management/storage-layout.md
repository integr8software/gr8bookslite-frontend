# Storage Layout (`WLY`)

Storage Layout configures the hierarchy of one selected warehouse.

Canonical route:

- `/warehouse-management/storage-layout`

The module supports warehouse-specific levels, duplicate, copy layout, and apply layout operations.
